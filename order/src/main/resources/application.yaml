server:
  port: 8083

spring:
  application:
    name: order

  datasource:
    url: jdbc:mysql://localhost:3307/orderdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false

  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
        enable.idempotence: true

    consumer:
      group-id: order-service-group
      auto-offset-reset: latest

      key-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer

      properties:
        spring.deserializer.key.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        spring.deserializer.value.delegate.class: org.springframework.kafka.support.serializer.JsonDeserializer

        # 🔥 REQUIRED (Order consumes DeliveryEvent)
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.fd.events.DeliveryEvent
        spring.json.trusted.packages: com.fd.events

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
logging:
  level:
    root: INFO

    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN

    org.springframework.orm.jpa: WARN
    org.springframework.data.jpa: WARN

    org.apache.kafka: WARN
    org.springframework.kafka: WARN

    com.netflix.discovery: WARN
    org.springframework.cloud: WARN

    com.fd: INFO
